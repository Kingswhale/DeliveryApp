<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="ng.nhub.deliveryapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme"
        android:largeHeap="true">
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity
            android:name=".Activity.activity.WelcomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.activity.SignUp"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".Activity.activity.SignIn"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity.activity.Home"
            android:label="@string/title_activity_home"
            android:theme="@style/MyMaterialTheme">
        </activity>
        <activity
            android:name=".Activity.activity.Order"
            android:label="@string/title_activity_order" />
        <activity
            android:name=".Activity.activity.SignInEmail"
            android:parentActivityName=".Activity.activity.SignUp"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden|adjustResize|adjustPan" />
        <activity
            android:name=".Activity.activity.SignUpFacebook"
            android:parentActivityName=".Activity.activity.SignUp"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.activity.ProfileUpgrade"></activity>
    </application>

</manifest>